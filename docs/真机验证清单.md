# 真机（DSHA / Android）验证清单

> 目的：本开发容器有**两个不可绕过的验证盲区**——
> ① `LD_PRELOAD` 惰性（外层 proroot 自研 loader 劫持 execve，见
> `docs/调查-LD_PRELOAD在proroot容器内失效.md`）；② bridge 链 SIGILL。
> 因此一批修复只能做到"代码层正确 + 符号级/源码级单元验证"，
> **hook 的运行时行为必须在此清单逐项过一遍**才能闭环。
>
> 用法：真机（DSHA 容器）上按顺序执行，每项记录 PASS/FAIL 与实测输出。
> 全部 PASS 后本清单可归档为"已闭环"。

---

## A. 进程与身份（fakeroot / D4 域）

| # | 项目 | 命令 | 期望 |
|---|---|---|---|
| A1 | 多线程账本并发 | `pnpm install`（真实多线程客户）或 `test/t_concur.c` 8 线程版 | 无崩溃、`/proc` 无残留、安装成功 |
| A2 | `id root` | `bxroot -0 id root` | `uid=0(root) gid=0(root) groups=0(root)` |
| A3 | by-id 查询 | `getent passwd 0`、`getent group 0`、`python3 -c "import pwd;print(pwd.getpwuid(0))"` | 与宿主/官方一致（修前 NULL） |
| A4 | comm | `bxroot /tmp/脚本`（脚本内 `cat /proc/self/comm`） | 脚本名前 15 字节（修前 `libproroot-brid`） |
| A5 | comm 子进程 | `bxroot sh -c '脚本'` | comm = 脚本名（不是 `sh`） |

## B. 路径语义

| # | 项目 | 命令 | 期望 |
|---|---|---|---|
| B1 | shebang argv[1] | `bxroot /tmp/sg`（`#!/bin/echo HELLO`） | `HELLO /tmp/sg`（修前带 `$ROOTFS` 前缀） |
| B2 | `/proc/self/fd/N` | 打开 rootfs 内文件后 `readlink /proc/self/fd/N` | guest 路径（无 `$ROOTFS` 前缀） |
| B3 | fd 祖先目录 | `test-51943658.c` 探针 | `openat(fd,"..")` 的 fd readlink = `/` |
| B4 | realpath 反向 | `python3 -c "import os;print(os.path.realpath('/etc/hostname'))"` | guest 路径 |
| B5 | symlink exec | `ln -s A B; bxroot B` | 正常执行，`$0` = `/tmp/B`（调用者给的） |
| B6 | cwd 继承 | `cd /x && bxroot -r R pwd -P` | `/x`（修前 `/`） |
| B7 | `realpath` 与 `getcwd` 一致 | `cd /x; bxroot sh -c 'test "$(pwd -P)" = "$(realpath .)" && echo SAME'` | `SAME` |

## C. CLI / 信号

| # | 项目 | 命令 | 期望 |
|---|---|---|---|
| C1 | `-v -1` 静默 | `bxroot -v -1 /bin/true` | 无任何 bxroot 输出，rc=0 |
| C2 | `-v 1` | `bxroot -v 1 /bin/true` | 有 launcher 诊断，rc=0 |
| C3 | 裸 `-v` | `bxroot -v /bin/true` | 同 C2（兼容双模式） |
| C4 | SIGPIPE 复位 | `bash -c "trap '' PIPE; bxroot -r R bash -c 'yes \| head -1 >/dev/null; echo \${PIPESTATUS[0]}'"` | `141`（修前 `1`） |
| C5 | `-b` 单路径 | `bxroot -r R -b /tmp/x cat /tmp/x/f` | 正常（上游 `path:path` 语义） |
| C6 | `BXROOT_NO_CRASH=1` | 置位后跑崩溃探针 | 无 bxroot 现场打印，客户 handler 接管 |
| C7 | `BXROOT_RAW_SYSCALL=1` | 容器内 `yes \| head -1` 及 io_uring 依赖程序 | 内核真实回答，无 ENOSYS 伪装 |

## D. NSS 与工具链

| # | 项目 | 命令 | 期望 |
|---|---|---|---|
| D1 | `getent passwd root` / `0` | 与官方对照 | 逐字一致 |
| D2 | `python pwd.getpwnam/getpwuid` | 与官方对照 | 一致（修前 KeyError） |
| D3 | `dpkg -i`（含 timestamp 警告预期） | 与官方对照 | 包状态一致 |
| D4 | `apt-get update` | 官方对照（**评估报告 3.7 的原始现场**） | partial ENOENT 是否复现/消失 —— 这是该报告项的唯一闭环手段 |
| D5 | `make` 简单工程 | `make`（含配方子进程） | 按已知限制 2.1 记录行为 |
| D6 | node | `node -e 'console.log(process.execPath)'` | `/usr/local/bin/node`（完整路径，修前 `node`） |

## E. 部署链（仅真机可验）

| # | 项目 | 期望 |
|---|---|---|
| E1 | bridge 链 argv0 | `ln -s A B; bxroot B` 的 `$0` 是否仍被 bridge 覆盖为解析后路径（本容器实测 `$0=/tmp/A`，调用者语义应为 `/tmp/B`）。若覆盖，需与官方确认 `--argv0` 契约 |
| E2 | `execve("/proc/self/exe")` | 自研 loader 是否仍报 `no PT_DYNAMIC`（上游 test-0cf405b0/25069c12/13） |
| E3 | 静态二进制 + LD_PRELOAD | 确认语义不变（上游 GNUmakefile 默认静态编译的用例集） |
| E4 | `test-311b7a95` 链式 shebang | `#!脚本B`（B 也是脚本）在 loader 下的行为 |
| E5 | 内核 4.14 vs 更新内核 | io_uring/clone3/openat2 等编号表按内核代际的差异 |

---

## 已在本容器闭环（无需真机重跑，供对照）

- 参数表 40 条目 vs 审计器实测（遗漏 25 → 0）
- fakeroot 账本并发（8×400 两阶段）
- `-v` 四形态解析、`--verbose=N`
- NSS by-name/by-id 直解（`RUN_NSS_EDGE` 10/10）
- D3 单元 15/15、realpath 单元 12/12、RAW_SYSCALL 26/26
- CLI 选项表 35/35、CLI 语义 38 项
- 上游套件移植：34 通过 / 17 失败（17 项全部归因：套件污染 5、
  bridge 边界 2、架构边界 4、proot 自身补偿 1、环境限制若干）

## 诚实标注

- 清单 A–D 的"期望"列以**上游 proot 开源语义**与**官方 proroot 实测**为准；
  若真机出现不一致，先分层归因（hook 未生效 / 修复未生效 / 判据过时）。
- E1 的 bridge `--argv0` 行为属于外层闭源组件，若确认覆盖，评估
  是否能通过 launcher 侧补偿（当前已有 `resolve_guest_symlinks` 先例）。
