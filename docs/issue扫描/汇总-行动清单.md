# proot 上游 issue 扫描汇总（2026-09-18）

> 数据源：proot-me/proot 全部 430 条（261 纯 issue），5 个子代理按主题域
> 并行扫描，每条相关 issue 给出 bxroot 适用性判定（已覆盖/缺口/不适用）。
> 分域明细见 `docs/issue扫描/`（路径翻译域 / 进程信号域 / 资源平台域，
> 身份权限域与程序兼容域到货后补充）。

## 高价值缺口行动清单（去重合并后）

> ⚠️ **状态列已于 2026-09-19 逐项复核并改正。**
> 原表把 9 项标为「🔧 修复中」，但复核结论是：**这 9 项当时在源码里
> 一行都没动**（判定依据是逐项 `grep` 源码 + 读调用点，不是看报告）。
> 「修复中」是个会误导读者的状态 —— 它让人以为不必再动手，
> 而实际上缺口原样存在。现已改为准确的「**未动手**」，并在下面给出
> 每项的证据位置与验证命令。

| # | 缺口 | 来源 issue | 真实状态（2026-09-19 复核） |
|---|---|---|---|
| 1 | **l2s getdents64 d_type 现形**：scandir 报 symlink 而 lstat 报 regular 自相矛盾，pnpm/Turbopack 生产失败 | #407 + #418 | ⏳ **未动手**（已复现：`readdir` 报 `DT_LNK` 而 `lstat+patch` 报 `S_IFREG`） |
| 2 | **l2s 相对路径断链**：git 仓库损坏根因，官方 430 条无同类（无上游解法可摘） | 本地最小复现 | ⏳ **未动手**（生产布局偶然缓解，见下注） |
| 3 | **kill-on-exit 孤儿进程**：仅 atexit 挂载，`_exit`/exit_group/信号致死不清理 | #97 引申 | 📋 设计中（launcher 监督进程方案，源码注释自认） |
| 4 | **SIGSYS 裸 syscall 屏蔽绕过**：裸 rt_sigprocmask 不被剔除 → rc=159 | #134 引申 | ⏳ **未动手**（且原文档号码 175/14 是错的，应为 135/134） |
| 5 | **AF_UNIX DGRAM 不翻译**：sendto/recvfrom 纯转发（bind/connect 已覆盖） | #8 残留 | ⏳ **未动手** |
| 6 | **stub-loader /tmp 硬编码**：noexec 时静默失败（#79 同类根因） | #79 | ⏳ **未动手**（且整条路径未接线，见下注） |
| 7 | **launcher 未设 PWD**：bind 目标==-w 时 bash pwd 回显宿主路径（官方 v4.0.1 解法即设 $PWD） | #64 | ⏳ **未动手** |
| 8 | **execve 失败诊断不足**：仅一行 strerror，缺上游式 4 条原因提示；execveat 无 ENOSYS 回退 | #402/#21 | 📋 待办 |
| 9 | **glib g_spawn_async 端到端回归缺失**（clone3 回退链的真实栈验证） | #357 | 📋 待办（属测试基础设施缺口；容器内无 glib） |
| 10 | **kernel 4.19 最低版本未验证/未声明** | #417 | 📋 待办 |

### 三个只有动手才会发现的约束（复核时实测得出）

- **#1 的隐藏约束**：glibc 的 `readdir` 用**本地 bl** 直调 `getdents64`
  （`objdump -d libc.so.6`：`bcc90: bl bcb80 <getdents64>`，不经 PLT）。
  所以**只 hook `getdents64` 符号抓不到 `readdir`** —— 必须同时 hook
  glibc 导出的 `readdir`/`readdir64`，并另在 `syscall_guard.c` 给
  裸 `syscall(61 /*getdents64*/)` 加结果缓冲改写（node/libuv 走裸 svc）。
  漏掉任一条 = 做出「看起来修了、pnpm 依然失败」的假修复。
- **#2 文档措辞不准确**：断链**只在非集中布局下成立**。实测两形态对照
  （真实 `l2s_rt_link`）：`l2s_dir=NULL`（散落布局）落盘的是**相对**
  target → 从父目录读 `ENOENT`；而 `l2s_dir=<rootfs>/.l2s`（launcher
  的默认生产路径，`launcher.c:1205-1218`）落盘是**绝对** target → 读得通。
  缺口仍在（`--link2symlink` 未开、直接经 bridge 跑、或显式清空
  `BXROOT_L2S_DIR` 时复现），但成因是「相对形态落盘」，不是普适的。
- **#6 的真实影响被「未接线」掩盖**：`patch_static_elf` **无任何调用者**
  （`main` 走 `execve + LD_PRELOAD` 回退），`BXROOT_STUB_LOADER` 只被
  launcher `setenv` 而运行时无消费者。所以「noexec 时静默失败」
  **今天不可达** —— 不是因为修好了，而是这条路径从未被走到。
  正确的顺序是**先接线，再修 /tmp**，否则修了也无人走到。

## 关键结构性结论

- **proot 的进程/信号痛点多属架构性免疫**：bxroot 无 tracer，launcher
  execve 直接落位，#146（信号退出码）、#11（daemon）、#60（作业控制）、
  #291/#292/#261（ptrace 仿真缺陷）天然不存在。
- **资源平台类**：#44（personality/JVM）、#213（ptrace TAB 补全）、
  #227（Heroku 禁 ptrace）等对 bxroot 结构性免疫或反成卖点。
- **官方 430 条中，l2s 相对路径断链无同类 issue** —— 这是 bxroot
  独有的缺陷面（来自 l2s 的磁盘形态设计），无上游解法可摘。

## 复核纪律

- 判定"已覆盖"必须给代码位置；"缺口"必须给验证命令；"不适用"必须给理由。
- 子代理报告中的"待验证"项（#307/#66、#421、#9）保留待真机或后续轮次。
